# Module Point de Vente

## Description
Module de gestion du point de vente (POS) pour ERP Zone.

## Démarrage

```bash
cd point_vente
pip install -r requirements.txt
uvicorn main:app --reload --port 8007
```
