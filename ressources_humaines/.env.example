# =============================================================================
# Variables d'environnement - Ressources Humaines
# =============================================================================
# Copiez ce fichier vers .env et adaptez les valeurs
# =============================================================================

# Configuration de la base de données
DATABASE_URL=postgresql://erpuser:erppassword@localhost:5432/erp_rh

# Configuration de l'application
SECRET_KEY=votre-cle-secrete-a-changer-en-production
DEBUG=false
PROJECT_NAME=ERP Zone - Ressources Humaines
PROJECT_VERSION=1.0.0
DESCRIPTION=Module de gestion des ressources humaines

# Configuration du serveur
HOST=0.0.0.0
PORT=8002

# Configuration CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Configuration JWT
JWT_SECRET_KEY=votre-jwt-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configuration SMTP (pour les emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@erpzone.com
SMTP_PASSWORD=your-smtp-password

# Configuration Redis (pour le cache)
REDIS_URL=redis://localhost:6379/0

# Configuration des logs
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
